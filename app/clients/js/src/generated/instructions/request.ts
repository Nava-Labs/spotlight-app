/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { Serializer, struct, u64 } from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RequestInstructionAccounts = {
  escrowVault: PublicKey | Pda;
  escrowSolVault: PublicKey | Pda;
  user: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RequestInstructionData = { solAmount: bigint };

export type RequestInstructionDataArgs = { solAmount: number | bigint };

export function getRequestInstructionDataSerializer(): Serializer<
  RequestInstructionDataArgs,
  RequestInstructionData
> {
  return struct<RequestInstructionData>([['solAmount', u64()]], {
    description: 'RequestInstructionData',
  }) as Serializer<RequestInstructionDataArgs, RequestInstructionData>;
}

// Args.
export type RequestInstructionArgs = RequestInstructionDataArgs;

// Instruction.
export function request(
  context: Pick<Context, 'programs'>,
  input: RequestInstructionAccounts & RequestInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'spotlightPrograms',
    'CgBcBA5wtFsHaSMwDqpoTwweqVarEb8XUMYiLstNJJXo'
  );

  // Accounts.
  const resolvedAccounts = {
    escrowVault: {
      index: 0,
      isWritable: true as boolean,
      value: input.escrowVault ?? null,
    },
    escrowSolVault: {
      index: 1,
      isWritable: true as boolean,
      value: input.escrowSolVault ?? null,
    },
    user: { index: 2, isWritable: true as boolean, value: input.user ?? null },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RequestInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRequestInstructionDataSerializer().serialize(
    resolvedArgs as RequestInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
